[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 300 web.app:app"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
